spring.datasource.url=jdbc:mysql://localhost:3306/quizdb?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=krishna
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server port (optional)
server.port=8080

# Enable H2 Console if needed
spring.h2.console.enabled=false

# ==============================================
# = JWT CONFIGURATION
# ==============================================
jwt.secret=MySuperSecretKey123456789
jwt.expiration=86400000   # 1 day in milliseconds (24 * 60 * 60 * 1000)
jwt.header=Authorization
jwt.prefix=Bearer 
